{
    "type": "postgres",
    "host": "localhost",
    "port": 5440,
    "username": "postgres",
    "password": "postgres",
    "database": "postgres",
    "synchronize": false,
    "logging": true,
    "entities": ["{src,dist}/entities/**/index.{ts,js}"],
    "migrations": ["{src,dist}/migrations/**/*.{ts,js}"],
    "subscribers": ["{src,dist}/subscribers/**/*.{ts,js}"],
    "cli": {
        "entitiesDir": "src/entities",
        "migrationsDir": "src/migrations",
        "subscribersDir": "src/subscribers"
    }
}
